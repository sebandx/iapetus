# Required for Cloud Functions framework
functions-framework==3.*

# Required for Firestore and Vertex AI integration
google-cloud-aiplatform = { extras = [
    "agent-engines",
], version = "^1.93.0" }
google-cloud-firestore
firebase-admin

# Required to decode Firestore event payloads (Protobuf messages)
google-events
